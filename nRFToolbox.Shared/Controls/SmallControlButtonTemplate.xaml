<UserControl
    x:Class="nRFToolbox.SmallControlButtonTemplate"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:nRFToolbox"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid HorizontalAlignment="Left">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!--<Canvas Height="32" Width="32" Grid.Row="0" Background="{Binding Background}">
        </Canvas>-->
        <StackPanel Grid.Row="0" Background="{Binding Background}">
            <Image Source="{Binding Image}" Height="32" Width="32"/>
        </StackPanel>
    </Grid>
</UserControl>
