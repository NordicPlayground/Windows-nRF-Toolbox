<UserControl
    x:Class="nRFToolbox.ToolboxHeaderLayout"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:nRFToolbox"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="65"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Style="{StaticResource ToolboxBlueBackgroundStyle}">
            <TextBlock x:Name="HeaderBar" VerticalAlignment="Bottom"  TextWrapping="Wrap" Text="nRF Toolbox" Margin="19,0,0,0" Foreground="White" Style="{StaticResource ToolboxTitleTextBlockStyle}"/>
        </StackPanel>
        <StackPanel Grid.Row="1">
            <Canvas>
                <TextBlock x:Name="ProfileHeader" HorizontalAlignment="Left"  TextWrapping="Wrap" Text="Profiles" Style="{StaticResource ToolboxHeaderTextBlockStyle}" Canvas.Left="19" />
            </Canvas>
        </StackPanel>
    </Grid>
</UserControl>
